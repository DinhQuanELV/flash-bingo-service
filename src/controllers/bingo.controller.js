const bingoController = {
  generate: async (req, res, next) => {
    // [GET] /bingo/generate/:moduleId?card=1
    try {
      //
    } catch (error) {
      next(error);
    }
  },
};

module.exports = bingoController;
